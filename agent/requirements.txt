fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.9.2
python-dotenv==1.0.1
python-multipart==0.0.17
httpx==0.27.2

# Auth
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pyjwt==2.10.1
email-validator==2.2.0

# LangChain / LangGraph
langchain==0.3.7
langchain-core==0.3.21
langchain-openai==0.2.9
langchain-community==0.3.7
langgraph==0.2.48
langsmith==0.1.147

# Vector / 检索
chromadb==0.5.20
langchain-chroma==0.1.4
rank-bm25==0.2.2

# 中文分词(BM25)
jieba==0.42.1

# 解析
pypdf==5.1.0
python-docx==1.1.2
beautifulsoup4==4.12.3
PyMuPDF==1.24.13  # PDF processing with better image extraction
Pillow==10.4.0  # Image processing

# DeepDoc / OCR (可选依赖，用于扫描件和复杂文档解析)
# 安装方式: pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
pytesseract==0.3.13  # Tesseract OCR wrapper
# paddleocr==2.8.1  # PaddleOCR (中文友好，依赖较重)
# paddlepaddle==2.6.2  # PaddleOCR dependency
opencv-python==4.10.0.84  # Image processing for OCR
langdetect==1.0.9  # Language detection

# OpenAI(语音/嵌入)
openai==1.55.2

# 数据库
sqlalchemy==2.0.35
asyncpg==0.30.0
aiosqlite==0.20.0

# 可选适配器(按需启用;不安装也能跑核心功能)
# langchain-milvus==0.1.7
# pymilvus==2.4.7
# elasticsearch==8.15.1
# neo4j==5.25.0
# sentence-transformers==3.3.1   # CrossEncoder 重排

# 多模型提供商 SDK (按需安装)
zhipuai==2.1.0          # 智谱 GLM
# dashscope==1.20.0     # 阿里云通义
# voyageai==0.2.4       # Voyage Embeddings
# cohere==5.11.0        # Cohere Rerank
# ollama==0.3.0         # 本地 Ollama
# langchain-ollama==0.2.0  # LangChain Ollama 适配
